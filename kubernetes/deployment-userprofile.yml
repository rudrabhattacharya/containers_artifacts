apiVersion: apps/v1
kind: Deployment
metadata:
  name: userprofile
  namespace: rudra
spec:
  replicas: 3
  selector:
    matchLabels:
      app: userprofile
  template:
    metadata:
      labels:
        app: userprofile
    spec:
      containers:
      - name: userprofile-api
        image: registryjrc4068.azurecr.io/rudra/userprofile:v1 
        resources:
          limits:
            memory: "256Mi"
            cpu: "1024m"
        env :
        - name: SQL_PASSWORD
          value: "kQ9pg2Kl9"
        - name: SQL_SERVER
          value: "sqlserverjrc4068"
        - name: SQL_USER
          value: "sqladminjRc4068"
        - name: SQL_DBNAME
          value: "mydrivingDB"
        ports:
        - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: userprofileapi
  namespace: rudra
spec:
  selector:
    app: userprofile
  ports:
  - port: 80
    targetPort: 80

